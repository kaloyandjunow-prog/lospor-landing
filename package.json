{
  "name": "lospor-landing",
  "version": "1.3.0",
  "private": true,
  "description": "Public landing page for the LOSPOR ecosystem.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "lint": "eslint .",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "wrangler": "4.114.0"
  }
}
